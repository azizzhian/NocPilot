# Salin ke /root/nocpilot-install.env (jangan commit) lalu:
#   sudo ./scripts/install.sh /root/nocpilot-install.env
#
# Alur disarankan: git clone dulu, baru jalankan install.sh (lihat DEPLOY.md)

# Wajib
NOCPILOT_DOMAIN=noc.example.com
DB_PASSWORD=ganti_password_kuat

# Opsional — kosongkan NOCPILOT_REPO jika sudah git clone
NOCPILOT_REPO=
NOCPILOT_PATH=/var/www/nocpilot
NOCPILOT_BRANCH=main
NOCPILOT_APP_NAME=NocPilot

# Database
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=nocpilot
DB_USERNAME=nocpilot

# 1 = install MariaDB lokal + buat database/user
INSTALL_MYSQL_LOCAL=1

# 1 = jalankan db:seed (user admin demo)
NOCPILOT_SEED=1

# Telegram (opsional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=

# 1 = pasang nginx site + systemd queue/scheduler
INSTALL_NGINX=1
INSTALL_SYSTEMD=1

# 1 = certbot Let's Encrypt (butuh DNS domain sudah mengarah ke VPS)
INSTALL_SSL=0
CERTBOT_EMAIL=admin@example.com

# PHP_FPM_SOCK=/run/php/php8.3-fpm.sock
